# Generated by Okty.io
version: '3.6'
services:
    mysql:
        image: 'mysql:5.7'
        environment:
            - MYSQL_DATABASE=${DB_NAME}
            - MYSQL_USER=${DB_USER}
            - MYSQL_PASSWORD=${DB_PASSWORD}
            - MYSQL_RANDOM_ROOT_PASSWORD=yes
    php:
        build: docker/php
        working_dir: '/app'
        volumes:
            - './:/app'
    composer:
        image: 'composer:1.8'
        command: install
        volumes:
            - './:/app'